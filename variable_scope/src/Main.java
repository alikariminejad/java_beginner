public class Main {
    public static void main(String[] args) {
        // local variable = declared inside a method and visible only to that method

        // global variable = declared outside a method, but within a class and visible to all parts of a class
        DiceRoller diceRoller = new DiceRoller();
    }
}